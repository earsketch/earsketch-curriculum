// Index Hatosi: To'g'irlash

setTempo(120);

var soundClip = DUBSTEP_DRUMLOOP_MAIN_006;
var measureTotal = readInput("Nechta o'lcham?");

println(measureTotal);

fitMedia(soundClip, 1, 1, Number(measureTotal) + 1);