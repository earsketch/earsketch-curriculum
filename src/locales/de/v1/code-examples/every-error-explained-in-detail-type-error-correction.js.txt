// Tippfehler: Korrektur

setTempo (120);

var soundClip = DUBSTEP_DRUMLOOP_MAIN_006;
var measureTotal = readInput("Wie viele Takte?");

println(measureTotal);

fitMedia(soundClip, 1, 1, Anzahl(measureTotal) + 1);