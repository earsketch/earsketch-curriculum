# Ausgeben: Benutze println() um Nachrichten in der Konsole auszugeben

# Setup
von EarSketch Import *
setTempo(120)

# Variablen
drum1 = OS_SNARE03
beat_string = "0---0---0-0-0---"

# Erste Ausgabeanweisung
print(1 + 3)

# Looping unseres Beats
# Beachte, dass sich die Druckanweisung in der for-Schleife befindet, so dass sie bei jeder Iteration der Schleife ausgeführt wird.
für Takte im Bereich(1, 5):
    makeBeat(drum1, 1, measure, beat_string)
    print(measure)
    print("ok")